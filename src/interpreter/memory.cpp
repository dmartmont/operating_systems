#include <iostream>
#include <string.h>
#include <vector>

using namespace std;

class Memory {
    private:

    public:
        Memory(string name, vector<string> segments) {
            cout << name << endl;
        }        
};